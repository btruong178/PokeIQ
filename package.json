{
  "name": "pokeiq",
  "version": "1.0.0",
  "description": "A Website that helps test the User's knowledge about Pokemon through Quizzes.",
  "main": "index.js",
  "scripts": {
    "start:frontend": "cd frontend && npm start",
    "start:backend": "cd backend && npm start",
    "start": "concurrently \"npm run start:frontend\" \"npm run start:backend\"",
    "ci:all": "npx concurrently \"npm --prefix frontend ci\" \"npm --prefix backend ci\"",
    "backend:prisma:generate:migratedev": "cd backend && npx prisma generate && npx dotenv -e ../.env.backend -- prisma migrate dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}